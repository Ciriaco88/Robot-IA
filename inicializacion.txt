Resumen para tu cuaderno de notas:

    Crear el entorno: ```bash
    python3 -m venv .venv

    Activar (El comando clave): ```bash
    sudo source .venv/bin/activate

    Instalar dependencias (Solo la primera vez): ```bash
    pip install python-dotenv google-generativeai
    
    python3 brain.py
    
    para acortar directorios de la terminal: PROMPT_DIRTRIM=2
